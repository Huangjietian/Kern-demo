<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE generatorConfiguration
        PUBLIC "-//mybatis.org//DTD MyBatis Generator Configuration 1.0//EN"
        "http://mybatis.org/dtd/mybatis-generator-config_1_0.dtd">

<generatorConfiguration>
    <!-- 引入数据库连接配置 -->
    <properties resource="application.properties"/>

    <context id="Postgresql" targetRuntime="MyBatis3Simple" defaultModelType="flat">
        <property name="beginningDelimiter" value="`"/>
        <property name="endingDelimiter" value="`"/>

        <!-- 配置 tk.mybatis 插件 -->
        <plugin type="tk.mybatis.mapper.generator.MapperPlugin">
            <property name="mappers" value="cn.kerninventory.demotkmybatis.base.BaseMapper"/>
        </plugin>

        <!-- 配置数据库连接 -->
        <jdbcConnection
                driverClass="${spring.datasource.driver-class-name}"
                connectionURL="${spring.datasource.url}"
                userId="${spring.datasource.username}"
                password="${spring.datasource.password}">
            <property name="nullCatalogMeansCurrent" value="true"/>
        </jdbcConnection>

        <!-- 配置实体类存放路径 -->
        <javaModelGenerator targetPackage="cn.kerninventory.demotkmybatis.model" targetProject="../demo-tkmybatis/src/main/java"/>

        <!-- 配置 XML 存放路径 -->
        <sqlMapGenerator targetPackage="mapper" targetProject="../demo-tkmybatis/src/main/resources"/>

        <!-- 配置 DAO 存放路径 -->
        <javaClientGenerator
                targetPackage="cn.kerninventory.demotkmybatis.domain"
                targetProject="../demo-tkmybatis/src/main/java"
                type="XMLMAPPER"/>

        <!-- 配置需要指定生成的数据库和表，% 代表所有表 -->
        <table tableName="biz_user">
            <!-- mysql 配置 -->
            <generatedKey column="id" sqlStatement="Postgresql" identity="true"/>
        </table>
        <table tableName="sys_country">
            <!-- mysql 配置 -->
            <generatedKey column="id" sqlStatement="Postgresql" identity="true"/>
        </table>
        <table tableName="sys_province">
            <!-- mysql 配置 -->
            <generatedKey column="id" sqlStatement="Postgresql" identity="true"/>
        </table>
        <table tableName="test_bill">
            <!-- mysql 配置 -->
            <generatedKey column="id" sqlStatement="Postgresql" identity="true"/>
        </table>
    </context>
</generatorConfiguration>